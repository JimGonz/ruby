<%= form_with(model: cine, local: true) do |form| %>
  <% if cine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cine.errors.count, "error") %> prohibited this cine from being saved:</h2>

      <ul>
      <% cine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :cine_name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :cine_phone %>
    <%= form.file_field :url_image, id: :cine_url_image %>
  </div>

  <div class="actions">
    <%= form.submit, class: 'btn btn-primary' %>
  </div>
<% end %>
